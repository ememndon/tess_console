# numpy<1.24 so basicsr's deprecated np.float usage keeps working.
numpy==1.23.5
opencv-python-headless==4.9.0.80
Flask==3.0.3
gunicorn==21.2.0
# GFPGAN + its stack. torchvision is pinned to 0.15.2 in the Dockerfile so
# basicsr's `from torchvision.transforms.functional_tensor import ...` still
# resolves (that module was removed in torchvision 0.17+).
gfpgan==1.3.8
basicsr==1.4.2
facexlib==0.3.0
realesrgan==0.3.0
